= Sharechain
:page-has_children: true
:page-nav_order: 24

A sharechain is a blockchain of shareblocks, and each shareblock embeds
a bitcoin header. The sharechain difficulty is much lower than the
bitcoin difficulty, so the sharechain is a blockchain of "weak" bitcoin
blocks.

== Shareblock Header and Bitcoin Header

The diagram below shows how the shareblock header relates to the
bitcoin header. The two `bits` fields hold one difficulty target each:
the bitcoin header's `bits` for the bitcoin block, and the
shareblock header's `bits` for the sharechain.

The shareblock header has other bookkeeping fields that the diagram
leaves out. The fields to note are the previous block hashes, the
`bits` fields, and the time fields. The bitcoin header is shown in
full.

[graphviz, shareblock-headers, svg]
.A shareblock's proof of work lives in its bitcoin header
....
digraph shareblock {
  compound=true
  newrank=true
  bgcolor="#0B0B0C"
  nodesep=0.15
  ranksep=0.15
  fontname="DejaVu Sans"
  fontcolor="#FFFFFF"
  node [shape=box, style=filled, fillcolor="#121212", color="#FFFFFF66",
        fontname="DejaVu Sans", fontsize=12, fontcolor="#FFFFFF",
        width=2.8, fixedsize=true, height=0.35]
  edge [color="#F7931A", fontcolor="#FFFFFFC7", fontname="DejaVu Sans",
        fontsize=10, penwidth=1.2]

  subgraph cluster_block {
    label="Shareblock n"
    fontsize=13
    color="#F7931A"
    style=filled
    fillcolor="#121212"

    subgraph cluster_share_header {
      label="Share header"
      fontsize=11
      fontcolor="#FFFFFFC7"
      color="#FFFFFF66"
      fillcolor="#121212"

      prevhash [label="previous shareblock hash"]
      sharebits [label="bits: sharechain target"]
      sharetime [label="time"]

      subgraph cluster_bitcoin_header {
        label="Bitcoin header"
        fontsize=11
        fontcolor="#FFFFFFC7"
        color="#FFFFFF66"
        fillcolor="#0B0B0C"

        version [label="version"]
        prevbtc [label="previous bitcoin block hash"]
        merkle [label="merkle root"]
        btctime [label="time"]
        btcbits [label="bits: bitcoin target"]
        nonce [label="nonce", fillcolor="#F7931A", color="#F7931A",
               fontcolor="#0B0B0C"]
      }
    }
  }

  prev [label="Shareblock n-1", color="#F7931A", width=1.6]
  pow [label="Proof of work\nhash of the bitcoin header", color="#F7931A",
       penwidth=2, width=2.6, height=0.6]
  powrule [shape=plain, style="", fillcolor="", fixedsize=false,
           fontcolor="#FFFFFFC7", fontsize=11,
           label="Meets sharechain target: valid share\lAlso meets bitcoin target: bitcoin block\l"]

  // Stack the fields top to bottom.
  edge [style=invis]
  prevhash -> sharebits -> sharetime -> version -> prevbtc -> merkle -> btctime -> btcbits -> nonce
  pow -> powrule
  edge [style=solid]

  {rank=same; prevhash; prev}
  {rank=same; merkle; pow}

  prevhash -> prev [label="points to", constraint=false]
  merkle -> pow [ltail=cluster_bitcoin_header, constraint=false]
}
....

== Sharechain and Bitcoin Blockchain

The sharechain is a chain of shareblocks. Each shareblock embeds a
bitcoin header that carries the proof of work. A node hashes the
bitcoin header and compares the resulting block hash with the
shareblock header's `bits` field to check that the share meets the sharechain
difficulty. It also compares the block hash with the bitcoin header's
`bits` field to check whether the share is a bitcoin block that it can
broadcast to the bitcoin network.

Several shareblocks can build on the same previous bitcoin block, as
the dashed arrows in the diagram below show. The orange arrow from
Shareblock 3 to Block 101 shows that Shareblock 3's proof of work meets
the bitcoin difficulty, so Block 101 can be submitted to the bitcoin
network.

[graphviz, sharechain-bitcoin-chain, svg]
.Shareblocks build on the bitcoin tip, and Shareblock 3 found Block 101
....
digraph sharechain {
  rankdir=LR
  newrank=true
  bgcolor="#0B0B0C"
  nodesep=1.4
  ranksep=0.45
  fontname="DejaVu Sans"
  fontcolor="#FFFFFFC7"
  node [shape=box, style=filled, fillcolor="#121212", color="#F7931A",
        fontname="DejaVu Sans", fontsize=12, fontcolor="#FFFFFF",
        width=1.3, height=0.45, fixedsize=true]
  edge [color="#F7931A", fontcolor="#FFFFFFC7", fontname="DejaVu Sans",
        fontsize=10, penwidth=1.2]

  subgraph cluster_sharechain {
    label="Sharechain"
    fontsize=12
    color="#FFFFFF66"
    s1 [label="Shareblock 1"]
    s2 [label="Shareblock 2"]
    s3 [label="Shareblock 3", fillcolor="#F7931A", fontcolor="#0B0B0C"]
    s4 [label="Shareblock 4"]
    s5 [label="Shareblock 5"]
    s6 [label="Shareblock 6"]
  }

  subgraph cluster_bitcoin {
    label="Bitcoin blockchain"
    labelloc=b
    fontsize=12
    color="#FFFFFF66"
    b100 [label="Block 100"]
    b101 [label="Block 101", fillcolor="#F7931A", fontcolor="#0B0B0C"]
    b102 [label="Block 102"]
  }

  // Each block points back to its parent.
  s1 -> s2 -> s3 -> s4 -> s5 -> s6 [dir=back]
  b100 -> b101 -> b102 [dir=back, minlen=2]

  {rank=same; s1; b100}
  {rank=same; s3; b101}
  {rank=same; s6; b102}

  // Previous bitcoin block hash in each shareblock's bitcoin header.
  edge [style=dashed, color="#FFFFFFC7", constraint=false]
  s1 -> b100 [xlabel="builds on    "]
  s2 -> b100
  s3 -> b100
  s4 -> b101
  s5 -> b101
  s6 -> b102

  // Shareblock 3's bitcoin header met the bitcoin target and became
  // Block 101, which already links it to Block 100.
  s3 -> b101 [style=bold, color="#F7931A", penwidth=2,
              xlabel="found block:      \lheader met\lbitcoin target\l"]
}
....

== Sharechain Provides Consensus on Payout Distribution

A sharechain gives all nodes Nakamoto consensus on the weak shares.
All nodes eventually converge on the same view of the sharechain, so
they all determine the same payout distribution when building shares.
Every node can therefore check that every share on the sharechain pays
out honestly. When a peer sends a share whose payouts do not match the
receiving node's local view, the receiving node rejects it. See
xref:pplns-accounting.adoc[PPLNS Share Accounting] for how a
decentralized network handles share accounting.

Without the consensus the sharechain provides, a node cannot validate
that a share's payout distribution matches its own view. See
xref:validation.adoc[Share Validation] for how shares are validated.

== Shares Have a Valuation

When the pool finds a block, every share within a certain depth of the
sharechain tip earns a payout from the bitcoin coinbase. The length of
the PPLNS (pay-per-last-N-shares) window sets this depth. A share
therefore holds an intrinsic value: it earns bitcoin if the pool finds
a bitcoin block before the share expires. This makes a share behave
like an option with a known expiry.

Apart from the share's position in the PPLNS window, other fields in
the shareblock help assign a value to a share:

. The pool difficulty the share was mined at: the shareblock header's `bits`
. The bitcoin difficulty when the share was mined: the bitcoin header's `bits`
. The share's height in the sharechain

Miners can sell this value to others. Miners want an immediate payout,
and buyers want the bitcoin the share will earn when the pool finds a
bitcoin block.

== Sharechain Provides UTXO & Script-Based Transaction Support

The sharechain supports UTXO and Script-based transactions. Each
shareblock has a coinbase that pays the share's miner one share unit.
This output captures the intrinsic value of the share, and transaction
support lets the miner trade it.

With UTXO and Script support, a miner can trade a share for bitcoin
through atomic swaps, on-chain or off-chain. See
xref:../payouts/marketplace.adoc[Sell Shares for Payouts] for how such
swaps give miners a payout mechanism on P2Poolv2.
