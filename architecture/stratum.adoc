= Stratum Server & Vardiff
:page-parent: Architecture
:page-nav_order: 10

Each p2poolv2 node comes with its own Stratum server, whose
responsibilities are to generate work for the mining rigs and to make
sure they work with a target share interval.

The stratum implementation is highly inspired by the params CKPool
uses. Specifically, 3s share interval and the vardiff algorithm
used. CKpool is one of the most robust and battle tested stratum
server implementation and p2poolv2 leverages the years of learning and
experience captured in the difficulty adjustment algorithm.

Our code base implements the stratum server in Rust and provides
extensive test coverage to ease future changes.

== Components

The diagram shows a simplified view of the various components of the
Stratum server.

[plantuml, stratum-components, svg]
.Stratum server components
....
@startuml
skinparam componentStyle rectangle
skinparam nodesep 30
skinparam ranksep 45

node "bitcoind" as bitcoind

rectangle "Stratum server" {
  component "Template fetcher" as gbt
  component "Job builder" as jobs
  component "Share validator" as validator
  component "Vardiff" as vardiff
}

database "Share chain" as chain
actor "Miners" as miners

bitcoind --> gbt : templates
gbt --> jobs
chain -up-> jobs : payouts, tip
jobs --> miners: notify
validator --> chain : pool shares
validator -up-> bitcoind : submitblock
jobs -- vardiff: update/get difficulty
@enduml
....

Template fetcher::
  Asks bitcoind for a new block template with `getblocktemplate`. It
  fetches one as soon as bitcoind announces a new block over ZMQ
  (`zmqpubhashblock`), and polls at a fixed interval in between so that
  new transactions make it into the work.

Job builder:: Turns each template into a mining job. The
  coinbase transaction pays out to miners according to PPLNS
  accounting over the share chain, and each job commits to the current
  share chain tip. Every connected miner is sent the new job with
  `mining.notify`.

Vardiff::
  Adjusts each miner's difficulty so that it submits roughly one share
  every 3.3 seconds, whatever its hashrate. It follows CKPool's
  algorithm and waits for enough shares, or enough time, before
  changing difficulty so that it does not overreact to luck.

Share validator::
+
--
Checks every submitted share against the miner's current difficulty,
and rejects duplicates. Then:

* a share that meets the *pool target* is sent to the share chain,
  which stores it and relays it to peers;
* a share that also meets the *bitcoin target* is a block, and is
  submitted to bitcoind straight away with `submitblock`.

Shares below the pool target still count towards the miner's measured
hashrate and vardiff, but do not enter the share chain.
--

[[miner-options]]
== Miner options

Miners pass options in the password field of `mining.authorize`, as
`key=value` pairs separated by commas, semicolons or spaces, for
example `d=65536,p2p=tp2pool1p...`.

[cols="1,3",options="header"]
|===
| Option | Effect

| `d=<difficulty>`
| Asks the stratum server to start at this difficulty instead of the pool default.
  Some miners know that their hashrate requires a high difficulty to start.

| `th=<terahashes>`
| Your miner's hashrate in TH/s, from which a starting difficulty is worked out.
  If both `d=` and `th=` are given, `d=` wins.

| `p2p=<address>`
| The P2Poolv2 address that owns your share chain outputs. Not needed
  if the node operator has set one for the whole pool.
|===

Suggested difficulties are kept within the pool's configured minimum
and maximum. See config documentation in p2poolv2 node's config.toml file.
